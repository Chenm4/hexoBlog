<!--&lt;!&ndash;这段代码是 ejs 模板语言中的代码，它实现了引入名为 "_page" 的局部模板，并将变量 post 赋值为 page，-->
<!--index 赋值为 false。其实现的功能是在当前模板中引入 "_page" 局部模板，并将 post 和 index 传递给它。&ndash;&gt;-->
<%- partial('_partial/_page', {post: page, index: false}) %>